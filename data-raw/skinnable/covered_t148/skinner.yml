textures:
  base.dds:
    modulate_regions:
      0,0, 1024,1024:
        saturation: 0.3
        brightness: 1.1
        mask: base_mask.png

    color_regions:
      0,0, 1024,1024:
        mask: base_mask.png
        comment: cab
        layer: cab_base

      336,330, 730,649:
        mask: base_bonnet_mask.png
        comment: bonnet (like cab stripe)
        layer: cab_stripe_base
